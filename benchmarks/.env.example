# Prisma Benchmarks Configuration

# Note: Prisma 7 uses prisma.config.ts for database URL configuration
# This .env file is kept for any other environment-specific settings

# Example: Custom configuration (if needed)
# NODE_ENV=production
